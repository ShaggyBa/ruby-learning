%h1 Список пользователей библиотеки

%table
  %thead
    %tr
      %th Email
      %th Password
      %th Role
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
    - next if user == current_user
      %tr
        %td= user.email
        %td= user.password
        %td= user.role
        %td= link_to 'Show', user if can? :read, user
        %td= link_to 'Edit', edit_user_path(user) if can? :update, user
        %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } if can? :destroy, user


%br

= link_to 'New User', new_user_path
